class Solution {
    public int missingNumber(int[] nums) {

        int len = nums.length;

        int count=0;
        for(int i=0; i<len; i++)
        {
            count += nums[i];
        }

        return (len*(len+1)/2) - count;
        
    }
}
